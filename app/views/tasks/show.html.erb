<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p>
  <%= @task.completed == true ? (check_box("tag", tag.id, {checked:@task.completed}) + " - The task is completed") : (check_box("tag", tag.id, {checked:@task.completed}) + " - The task is not completed") %>
</p>
<p><%= link_to("Back to the list", tasks_path) %></p>